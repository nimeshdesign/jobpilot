# Dependencies for the hosted (Vercel) deployment.
# Playwright is deliberately absent: Chromium is ~430 MB against a 250 MB
# function limit, and a serverless function cannot hold a browser open for a
# multi-minute apply run. Form autofill stays on your own machine.
fastapi==0.115.6
python-multipart==0.0.20
sqlalchemy==2.0.36
pydantic==2.10.4
httpx==0.28.1
python-dotenv==1.0.1

pdfplumber==0.11.4
python-docx==1.1.2
beautifulsoup4==4.12.3
lxml==5.3.0
reportlab==4.2.5

# Postgres driver: the filesystem is ephemeral, so SQLite cannot be used.
psycopg[binary]==3.2.3
